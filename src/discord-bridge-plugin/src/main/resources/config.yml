# Discord Bridge Plugin Configuration

# HTTP API port for receiving commands from .NET services
http-port: 8180

# Bridge API URL for /goto command
bridge-api-url: "http://localhost:5169"

# Guild/server name shown in welcome title
guild-name: "Discord World"

# Redis connection settings
redis:
  host: "localhost"
  port: 6379
